# Plan 03.5: MCCL Rank-Arrival Attribution Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `executing-plans` to
> implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for
> tracking.

**Status (2026-07-14): not started. This is an attribution and routing gate,
not a communication optimization plan.**

**Goal:** Determine whether the TP=4 MCCL residency exposed after the Plan 02
MHC optimization is caused by rank-arrival skew, actual collective transfer,
or graph/host gaps, then route work to Plan 04, 05, or 06 without guessing.

**Architecture:** Parse the four rank traces by steady decode step and
all-reduce ordinal, calculate rank start/end alignment, and correlate each
collective with its immediately preceding device and host events. Reproduce
the classification across three unchanged-workload captures before promoting
an existing optimization plan.

**Tech Stack:** MetaX C500, MCCL, PyTorch profiler JSON traces, TP=4 vLLM,
PIECEWISE CUDA Graph, Python structured trace analysis.

## Global Constraints

- Read `00-shared-contract.md` before execution.
- Acceptance remains TP=4; TP=1 is diagnosis-only and cannot satisfy this plan.
- Keep Plan 02 exact-on, MTP off, PIECEWISE graph, prefix caching on,
  `MAX_TOKENS=100`, `MAX_MODEL_LEN=512`, and `GPU_MEM=0.9`.
- Do not reduce TP, output length, graph mode, or workload difficulty.
- Do not modify MCCL algorithms, model kernels, or graph scheduling during the
  attribution captures.
- Do not interpret profiler kernel residency as pure link-transfer time.
- Keep profiler throughput separate from normal serving throughput.
- Require intended native dispatch, exact 100-token IDs, and no fallback.
- Plan 04, 05, and 06 remain unstarted until this gate selects one of them.

## Frozen Starting Evidence

The accepted Plan 02-on normal baseline is `30.640709 TPS` with median latency
`3.263632 s`; TP=4 23-token and 100-token IDs pass byte-for-byte without
fallback. The corresponding artifacts are:

- `.logs/mhc_downstream_sinkhorn_parallel_main_e2e_20260714/`
- `.logs/mhc_downstream_sinkhorn_parallel_main_final_20260714/`

The before/after profiler evidence is:

| Evidence | Rank 0 MCCL | Other ranks | Interpretation |
| --- | ---: | --- | --- |
| Before MHC optimization | `6.599 ms / 435` | `7.350 / 5.366 / 5.856 ms` | Balanced residency was small |
| After MHC optimization | `47.968 ms / 435` | `48.533 / 47.882 / 3.289 ms` | Rank-asymmetric wait or arrival skew |

The after trace does **not** prove that the interconnect is slow. Ranks 0-2
spending about `48 ms` in MCCL while rank 3 spends `3.289 ms` is synchronization
evidence that must be decomposed into arrival wait and post-arrival completion.

Source artifacts:

- `.logs/plan02_on_decode_profile_20260714/`
- `.logs/mhc_downstream_sinkhorn_parallel_after_profile_20260714/`

## Metric Contract

For steady decode step `s`, collective ordinal `i`, and rank `r`, first match
the semantically identical GPU graph-parent range on every rank. Use its start
as a per-rank local anchor:

```text
anchor[s,r]          = matched GPU graph-parent start
relative_start[s,i,r] = collective start - anchor[s,r]
relative_end[s,i,r]   = collective end - anchor[s,r]
arrival_skew[s,i]     = max(relative_start) - min(relative_start)
completion_tail[s,i]  = max_r(max(0, relative_end[r] - max(relative_start)))
residency[s,i,r]      = end - start
pre_gap[s,i,r]        = start - end(previous device event on the same rank/stream)
late_rank_candidate   = rank with maximum relative_start
```

Subtracting a same-rank graph anchor removes constant cross-process clock
offsets while retaining accumulated rank-local compute and scheduling skew
inside the matched decode graph. Require identical graph-parent semantics,
collective counts, and collective ordinals across ranks. If graph parents
cannot be matched, the run is inconclusive.

Residency asymmetry remains supporting evidence only. It may show that some
ranks wait longer, but it cannot identify arrival order or route a plan by
itself.

Cross-rank absolute timestamps are optional corroborating evidence. Use them only when
all four traces expose the same absolute profiler clock metadata and a shared
calibration marker proves residual offset `<=2 us`. Under that condition:

```text
arrival_skew = max(start) - min(start)
completion_tail = max_r(max(0, end[r] - max(start)))
```

If calibration is absent or fails, report absolute start/end alignment as
unavailable and continue with graph-anchor-relative metrics; never route from
uncalibrated absolute timestamps.

Also record the preceding operator category for every rank:

```text
MHC | MoE | attention | memcpy | graph/host gap | other
```

Do not sum overlapping asynchronous parent and child events. Match collectives
only when all four ranks have the same steady-step and ordinal counts.

## Task 1: Freeze a Reproducible Trace Manifest

**Files:**

- Create: `.logs/plan35_mccl_rank_skew_20260714/workload_manifest.txt`
- Create: `.logs/plan35_mccl_rank_skew_20260714/import_manifest.txt`

**Interfaces:**

- Consumes: the accepted main-tree Plan 02-on binary and normal baseline.
- Produces: the immutable workload and binary identity used by all captures.

- [ ] **Step 1: Record repository and binary identity**

  Record Git status/HEAD, loaded module paths, `_metax_sparse_C` path, size,
  mtime, SHA256, model path, and MACA/PyTorch versions.

- [ ] **Step 2: Copy the frozen serving parameters**

  Inherit the exact settings from
  `.logs/mhc_downstream_sinkhorn_parallel_main_e2e_20260714/benchmark/run_manifest.json`.
  Explicitly record TP, MTP, graph mode, prompt, token counts, prefix cache,
  batched-token limit, GPU memory, warmups, repetitions, and profiler window.

- [ ] **Step 3: Verify the uninstrumented control**

  Reuse the frozen 100-token oracle and require all IDs to match. Do not replace
  the accepted `30.640709 TPS` normal baseline with profiler throughput.

## Task 2: Build the Four-Rank Arrival Analyzer

**Files:**

- Create: `tools/debug/analyze_tp_collective_arrivals.py`
- Create: `tests/tools/test_analyze_tp_collective_arrivals.py`

**Interfaces:**

- Consumes: four gzip or plain PyTorch profiler JSON trace files.
- Produces: `collectives.jsonl`, `summary.json`, and `summary.csv` with the metric
  contract above and explicit unmatched-event diagnostics.

- [ ] **Step 1: Write synthetic trace tests**

  Cover equal graph-relative arrivals, one late rank, long completion tail,
  graph-gap attribution, different constant clock offsets, mismatched graph
  anchors, nested parent/child events, missing ranks, and mismatched collective
  counts. Require the analyzer to reject incomplete four-rank matches instead
  of silently aligning the wrong calls.

- [ ] **Step 2: Run the tests and establish the red signal**

  Run:

  ```bash
  source .venv/bin/activate
  source ./env.sh
  python -m pytest -q tests/tools/test_analyze_tp_collective_arrivals.py
  ```

  Expected before implementation: failure because the analyzer does not exist.

- [ ] **Step 3: Implement structured trace parsing**

  Parse JSON rather than grepping trace text. Group by rank, steady
  `execute_context`, GPU graph parent, stream, and all-reduce ordinal. Derive
  expected count as `5 active steps * 87 all-reduces = 435` per rank. Calculate
  arrival metrics from timestamps relative to the matched graph-parent start.
  Preserve raw and relative timestamps and event names in the output so every
  aggregate can be audited.

- [ ] **Step 4: Implement operator attribution**

  Select the immediately preceding non-overlapping device event on the same
  stream and the nearest enclosing graph/host range. Implement optional absolute
  clock calibration from profiler metadata plus a shared calibration marker;
  reject absolute cross-rank metrics when residual offset exceeds `2 us`.
  Report ambiguity when multiple candidates overlap; do not force an operator
  label.

- [ ] **Step 5: Validate the tool**

  Run the focused tests, Ruff checks, `py_compile`, and `git diff --check`.

## Task 3: Capture Three Unchanged TP=4 Profiles

**Files:**

- Create: `.logs/plan35_mccl_rank_skew_20260714/run1/`
- Create: `.logs/plan35_mccl_rank_skew_20260714/run2/`
- Create: `.logs/plan35_mccl_rank_skew_20260714/run3/`

**Interfaces:**

- Consumes: the frozen manifest from Task 1.
- Produces: three independent four-rank traces with the same active window.

- [ ] **Step 1: Run each profile through the official wrapper**

  Use the repository environment and the frozen workload with:

  ```bash
  PROFILE_DIR=/root/vLLM-metax/.logs/plan35_mccl_rank_skew_20260714/run1/trace \
  PROFILE_DELAY_ITERATIONS=4 \
  PROFILE_MAX_ITERATIONS=5 \
  PROFILE_ACTIVE_ITERATIONS=5 \
  PROFILE_IGNORE_FRONTEND=1 \
  NUM_SPECULATIVE_TOKENS=0 \
  MAX_TOKENS=100 \
  ./tools/run_deepseek_v4_mtp_generate.sh
  ```

  Use a distinct `PROFILE_DIR` ending in `run2/trace` and `run3/trace` for the
  other captures. Set all remaining workload variables explicitly from the
  Task 1 manifest. A run without four trace files under its `PROFILE_DIR` fails.

- [ ] **Step 2: Verify every capture**

  Require four rank traces, five active steady decode ranges, 100 correct token
  IDs, Plan 02 exact fail-closed dispatch, and no fallback. A trace with a
  changed workload or missing rank is invalid, not a partial pass.

- [ ] **Step 3: Run the analyzer on all captures**

  A routing-quality run requires exactly 87 collectives in every active step and
  435 per rank, with every step/ordinal matched across all four ranks. Preserve
  unmatched events and reasons, but any missing or ambiguous match invalidates
  that run for routing. All three runs must be complete; otherwise choose the
  inconclusive route.

## Task 4: Classify the Bottleneck

**Files:**

- Create: `.logs/plan35_mccl_rank_skew_20260714/comparison.json`
- Create: `.logs/plan35_mccl_rank_skew_20260714/decision.md`

**Interfaces:**

- Consumes: three analyzer summaries.
- Produces: exactly one route decision or an explicit inconclusive result.

- [ ] **Step 1: Test the rank-arrival hypothesis**

  Classify a run as arrival-wait dominated only when median `arrival_skew` is
  at least `1.0x` median `completion_tail` and P90 `arrival_skew` is at least
  `0.75x` P90 `completion_tail`. Require all three captures to satisfy this; a
  two-of-three result is inconclusive.

- [ ] **Step 2: Correlate late arrival with the preceding stage**

  For the top decile of `arrival_skew` events, report the late-rank candidate
  and its preceding operator. A Plan 04 or 06 route requires the same operator
  family to explain more than `50%` of those events in every capture. Ties,
  ambiguous labels, or different winners across captures are inconclusive.

- [ ] **Step 3: Test the transfer-tail hypothesis**

  Classify communication as dominant only when median `arrival_skew` is at
  most `0.25x` median `completion_tail`, P90 `arrival_skew` is at most `0.50x`
  P90 `completion_tail`, and the non-overlapping time union of all matched
  completion-tail intervals is at least `10%` of the active device window in
  every capture. Never sum overlapping collective intervals.

- [ ] **Step 4: Report confidence and limitations**

  Include median/P90 arrival skew, completion tail, pre-gap, per-rank residency,
  matched-call count, late-rank distribution, preceding-operator distribution,
  and cross-run variability. Include calibrated arrival skew/completion tail
  only when the clock gate passes. Route precedence is deliberately absent: if
  more than one route predicate passes, choose inconclusive.

## Task 5: Route to the Existing Plan

**Files:**

- Modify: `docs/superpowers/plans/deepseek-v4-c500-e2e/README.md`
- Modify: exactly one of `04-w4a16-moe.md`, `05-tp-communication.md`, or
  `06-cudagraph-launch.md` when a route passes.
- Modify: `docs/superpowers/plans/deepseek-v4-c500-e2e/03.5-mccl-rank-skew.md`

**Interfaces:**

- Consumes: the Task 4 decision.
- Produces: the next executable optimization plan and its evidence-backed entry
  condition.

- [ ] **Route A: Plan 04 — W4A16 MoE**

  Select only when the arrival-wait predicate passes and MoE precedes more than
  `50%` of top-decile skew events in every capture. Carry the evidence into
  Plan 04.

- [ ] **Route B: Plan 05 — TP communication**

  Select only when the communication predicate passes in every capture. Then
  benchmark the exact message sizes and call sites with
  `tools/debug/bench_tp4_allreduce.py` before changing MCCL configuration or
  implementing a custom collective.

- [ ] **Route C: Plan 06 — CUDA Graph launch**

  Select only when the arrival-wait predicate passes and graph/host gap precedes
  more than `50%` of top-decile skew events in every capture, with no tied
  compute category.

- [ ] **Route D: Inconclusive**

  Do not promote any plan. Record the missing evidence and add only the minimum
  instrumentation needed to distinguish the remaining hypotheses.

- [ ] **Final verification**

  Run Markdown lint on edited documents and `git diff --check`. Preserve all raw
  traces, analyzer outputs, manifests, and decision evidence.
